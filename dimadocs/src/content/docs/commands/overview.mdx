---
title: Commands Overview
description: Everything about DomDimaBot's default and custom commands.
sidebar:
  order: 2
---

import { Aside, Tabs, TabItem } from '@astrojs/starlight/components';

Commands are the foundation of DomDimaBot. They are split into two categories: **Default Commands** (built-in) and **Custom Commands** (created by you).

## Built-in commands

The built-in command catalog, seed enablement, usage syntax, and behavior live in the [Built-in command reference](/commands/built-in/). The shipped defaults seed enabled entries except `!s` and `!clearchat`; channel settings can differ. Use `!commands` to see the commands available to you in a particular channel.

This page focuses on streamer-created commands and repeating chat timers. For variables, functions, conditions, and command references, use the [AST guides](/commands/advanced/).

## Custom Commands

Custom commands are created by you and can send text, use AST functions, store data, trigger media, and call other commands.

<Aside type="tip">
Check the [Dashboard](/dashboard) page for a visual guide to creating and managing custom commands through the web interface.
</Aside>

### How to Create Custom Commands

You can create custom commands in two ways:

<Tabs>
<TabItem label="Via Chat">

Use the `!cc` command directly in chat:

```
!cc <command_name> <response>
```

The command name is created **without** the `!`.

**Example:**
```
😎Streamer: !cc discord Check out my Discord! discord.gg/example
🤖 DomDimaBot: Command !discord created!
```

With options:
```
!cc -cd=300 -ul=tier1 discord Sub-only Discord link! discord.gg/subonly
🤖 DomDimaBot: Command !discord created with custom settings!
```

| Option | Description |
|--------|-------------|
| `-cd=` | Cooldown in seconds. The minimum depends on the channel owner’s plan: Free `5`, Premium `3`, Pro `1`. |
| `-ul=` | User level, by number `1`–`10` or name: `1`/`everyone`, `2`/`tier1`, `3`/`tier2`, `4`/`tier3`, `5`/`vip`, `6`/`founder`, `7`/`mod`, `8`/`editor`, `9`/`admin`, `10`/`broadcaster`. `streamer` is an alias for `broadcaster`. `!cc -ul=7` and `!cc -ul=mod` are the same. |

**Editing:** `!ec <name> <new response>`  
**Deleting:** `!dc <name>`

</TabItem>
<TabItem label="Via Dashboard">

Navigate to the **Commands** section in your dashboard for a visual editor with:

- Full command list with enable/disable toggles
- Cooldown and permission settings
- Response editor for command text / AST templates
- Timer management

</TabItem>
</Tabs>

### Basic vs Advanced Custom Commands

There are two good ways to think about custom commands:

| Type | Best for | Example |
|------|----------|---------|
| **Basic text commands** | links, rules, simple info | `!discord`, `!rules`, `!schedule` |
| **AST commands** | dynamic replies, counters, logic, triggers | `!uptime`, `!hug`, loyalty systems |

### Important AST Note

DomDimaBot does **not** use placeholder syntax like `{user}` or `{game}` for advanced command logic.

Use:

- `$(user)` for the current user
- `$(twitch.game)` for the current game
- `&p1`, `&p2`, `&t` for arguments
- `%(...)` for variables you create yourself

## Repeating Chat Timers

Moderators can manage repeating chat messages with `!cct`, `!ect`, and `!dct`, or with `!timer create|edit|delete|list`. Timers send only while the channel is live. Timer messages can use AST syntax.

```text
!cct socials 15 Check out the channel links!
!ect socials 30 Updated links are in the panels.
!dct socials
!timer list
```

Timer names can contain letters, numbers, and underscores, up to `30` characters. Messages are limited to `350` characters. Active timer limits are Free `5`, Premium `15`, and Pro `50`.

- Free: `15`, `30`, `45`, or `60` minutes
- Premium: multiples of `5` from `5` to `180` minutes
- Pro: any whole minute from `1` to `180`

Enter a whole number of minutes, or use an `m` or `h` suffix (for example `15m` or `1h`). Editing an existing timer must use an interval allowed by the current plan. A timer with an interval no longer allowed after a plan change can keep running, but must be changed to an allowed interval before it can be edited.

## Examples of Correct Custom Command Patterns

<Tabs>
<TabItem label="Text Command">

Plain text responses for info, links, and announcements.

```
!cc discord Check out my Discord! discord.gg/example
```

**Chat example:**
```
💬 Viewer: !discord
🤖 DomDimaBot: Check out my Discord! discord.gg/example
```

```
!cc rules Please be kind and have fun!
```

</TabItem>
<TabItem label="Functions">

Dynamic replies should use AST functions.

**Example:**
```
!cc gameinfo $(twitch.channel) is currently playing $(twitch.game)
```

**Chat example:**
```
💬 Viewer: !gameinfo
🤖 DomDimaBot: DomDima is currently playing Valorant
```

</TabItem>
<TabItem label="Arguments">

Use `&p1`, `&p2`, and `&t` when you want input from the user.

**Single argument:**
```
!cc hug $(user) sends a warm hug to &p1! 🤗
```

**Chat example:**
```
💬 Viewer: !hug @FriendUser
🤖 DomDimaBot: Viewer sends a warm hug to @FriendUser! 🤗
```

**Trailing text:**
```
!cc say $(user) says: &t
```

**Chat example:**
```
💬 Viewer: !say this bot is amazing
🤖 DomDimaBot: Viewer says: this bot is amazing
```

</TabItem>
<TabItem label="Stored Data">

AST variables are created by you. They are not built in by default.

```
!cc wins *(^(#wins) ? %(#wins *(%(#wins) + 1)) : %(#wins 1)) $(user) now has %(#wins) wins!
```

**Chat example:**
```
💬 Viewer: !wins
🤖 DomDimaBot: Viewer now has 1 wins!
```

On the next use:

```text
💬 Viewer: !wins
🤖 DomDimaBot: Viewer now has 2 wins!
```

</TabItem>
<TabItem label="Triggers & Chaining">

You can trigger configured media and call other commands from AST.

**Trigger example:**
```
!cc airhorn $(trigger.send airhorn) AIRHORN!
```

**Chat example:**
```
💬 Viewer: !airhorn
🤖 DomDimaBot: AIRHORN!
```

If a trigger named `airhorn` exists, it will fire when the command runs.

**Command reference example:**

```
!cc creator Check out my links here: #(discord)
```

This runs the `discord` command inside `creator`.

</TabItem>
</Tabs>

### Advanced Custom Commands

The examples above only cover the safe basics. The full AST system supports:

- **Conditions** with `*(...)`
- **Variables and storage** with `%(...)`
- **Existence checks** with `^(...)`
- **Arguments** with `&p1`, `&p2`, and `&t`
- **Command references** with `#(...)`
- **Loops, arrays, and advanced logic**
- **Function calls** for Twitch, moderation, TTS, triggers, EventSub, and more

👉 For the full and correct AST reference, use these pages:

- **[Advanced Custom Commands](/commands/advanced)**
- **[AST Syntax & Data](/commands/advanced/syntax)**
- **[AST Functions Reference](/commands/advanced/functions)**
- **[AST Execution, Errors & Limits](/commands/advanced/execution)**
- **[AST Recipes & Patterns](/commands/advanced/recipes)**

---

*To create custom commands, use the `!cc` command in chat or visit the Commands section in your dashboard.*
